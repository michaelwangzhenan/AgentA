---
description: AgentA 项目工程公约 — 代码注释 / 文件命名 / 测试 / 文档同步 / AI 决策边界 / AI ↔ 用户交互节奏。所有针对本仓库的代码生成与修改都要遵守。
alwaysApply: true
---

# AgentA 工程公约

本项目是个人学习用 RAG + Agent 实验工程，作者优先级：**简洁可读 > 全面**。所有为本仓库写代码或文档的 AI agent / 协作者都需按本规则执行。**违反时被简化掉，而不是被讨论。**

权威定义在仓库文档：

- 代码规范以本文件为准
- **设计文档**（`docs/design.md`）风格详 [§3.0](../../docs/design.md#30-设计文档风格)
- **实现文档**（`docs/iter_2.md`）风格详 [§4.9.0](../../docs/iter_2.md#490-实现文档风格)

冲突时以这两份为准；本文件是"操作指引"，docs 是"风格约定"。

---

## 1. 代码注释

### 1.1 配置项（`src/config.py` 中的 `os.getenv` 行）

只写**配置项本身的意义和可选值**，不写设计思想 / 历史背景 / Phase 来源。每项 1-2 行注释足够。

```python
# ✅ 好：说明意义 + 类型 / 默认行为
# 是否启用项目级 rules 注入（可选值：true / false）
USER_RULES_ENABLED: bool = os.getenv("USER_RULES_ENABLED", "true").lower() == "true"

# rules 文件路径（相对项目根；文件不存在静默跳过）
USER_RULES_FILE: str = os.getenv("USER_RULES_FILE", ".agenta/rules.md")
```

```python
# ❌ 差：解释设计思想 / Phase / 跟其他模块的关系
# 项目级用户偏好（详 iter_2.md §4.9.3）：在项目根放 .agenta/rules.md，启动时一次性
# 加载并注入到 system prompt 的 <project_rules> 块。与 user_memory（动态学到的偏好）
# 形成两层：rules 是稳定基础设定，memory 在其之后注入做临时覆写。
USER_RULES_ENABLED: bool = ...
```

设计思想放在 `docs/design.md` 的对应章节，Phase 来源放在 `docs/iter_2.md` 的 §4.9.x；config.py 里只写"这个开关是干什么的"。

### 1.2 函数 / 类 docstring

- **函数**：1 句话讲做什么；参数 / 返回 / 异常需要时再列，不需要时不要硬凑
- **类**：1-2 句话讲职责边界；不复述模块 docstring
- 避免"逐行复述代码"的 docstring，例如 `def upsert(...)` 写成 "Upserts a record"

### 1.3 行内注释

- 解释**为什么**这么写，不解释**什么代码做了什么**
- ❌ `# 自增计数器` `# 调用 LLM` `# 返回结果`
- ✅ `# 显式触发不消耗也不重置 auto 计数器 —— 两条流水线独立`

### 1.4 中英文

注释默认中文（用户母语 + 工程语境）；行内 code identifier / 错误消息 / 库术语用英文（`config` / `tokenizer` / `prompt injection`）。

### 1.5 用词通俗（代码注释 + 文档 + 对话回复都适用）

避免拗口、过度抽象的工程化词；能用日常词就不用术语：

| ❌ 拗口 | ✅ 通俗 |
|---|---|
| 契约 | 约定 / 规则 |
| 编号契约 | 编号规则 |
| 落盘 | 存储 / 写入 |
| 强约束 | 必须 / 强制 |
| 闭环 | 完整流程 / 跑通；标题里 "评估闭环" 一律改 "评估方法" |
| 卡口 | 检查点 |
| 雏形 | 初稿 |

例外：**架构层面**的"接口约定"等已成行业术语场景可保留（如 design.md 描述 Protocol 设计），但日常描述用通俗词。

**借喻禁区**（专有术语不要跨语境借用）：

| 词 | 严格语义（仅可用于） | 误用借喻（一律禁止） | 正确替代 |
|---|---|---|---|
| `阻塞 / 非阻塞` | 线程 / I/O 是否同步等待（blocking I/O / `await`） | 借喻"业务流程是否同轮执行" / "是否等用户确认" | `同轮执行 / 分轮执行`、`两阶段（two-stage）`、`ack-only`、`是否需要用户审批` |
| `同步 / 异步` | 线程 / 并发模型 | 借喻"是否实时返回"、"是否对齐进度" | `立即返回 / 延后返回`、`实时 / 周期性` |
| `cap` | 代码层标识符（`MAX_*_CAP` / `xxx_cap` 变量名） | 中文叙述里裸用 `cap`、"小 cap 拦掉" | `上限`、`轮次上限`、`tool 上限`、`总上限` |

判定准则：你描述的是**操作系统 / 并发原语**层面的等待？是 → 用；否 → 换通俗词。代码 identifier 保持英文，但中文文档 / 注释里指代其语义时一律换通俗词。

### 1.6 术语边界：评估 vs UT

| 词 | 严格含义 | 在文档里出现的位置 |
|---|---|---|
| **评估** | `tools/rag_eval/` / `tools/agent_eval/` 下脚本，跑真实 LLM / 数据集，报告存储 `tools/*_eval/reports/<feature>-*.md` | iter_2.md Step 5；design.md "评估方法" 表 |
| **UT** | `tests/test_*.py`，编码顺带的强制流程，**与"评估"完全分开**，不出现在"评估"语境 | iter_2.md Step 4；design.md 子节脚注 |

约束：

- **UT 不算评估**：design.md §3.x "评估方法" 表**只列 tools/* 行**；UT 文件名以子节首一行说明带过即可（如 "正确性单测见 `tests/test_xxx.py`，编码顺带跑，不在本节评估范围"）
- **iter_2.md Step 4 / Step 5 严格分开**：Step 4 只写 UT 数字；Step 5 只写评估命令 + 通过率
- "评估"作为日常中文动词（"评估必要性" / "缺口评估"）跟本规则无关，自由用

### 1.7 写文档不要想当然（让读者读懂）

读者不一定按你写文档的顺序看，跳进任意一节都该能读懂。任何"心照不宣"的代号 / 缩写都得给出处。

| 反模式 | ❌ 想当然写法 | ✅ 显式上下文写法 |
|---|---|---|
| **跨节代号孤儿** | "承载 C3 的 quiz_maker..."（C3 在另一节定义） | "承载学习/研究助理（Phase 2 业务）的 quiz_maker..." 或 "承载 [§4.7.1 项目定位](#471-项目定位) C3 的..." |
| **同字母两次占不同含义** | §4.6 定义 C1-C7 是 Session 维度；§4.7 又拿 C1/C3 当业务场景 | 同一文档内**一组字母只定义一次**；要再分类换前缀（`S1/S2`） |
| **泛用术语无范围** | "对照 Step 0 验收"（文档里 Step 0 出现 20+ 次） | "对照 [§4.9.5 Step 0](#495-skills-框架强化-phase-15) 验收"；同节内紧邻段可省 |
| **章节标题带未定义代号** | `## 5.1. C4 企业内 Q&A` | `## 5.1. 企业内 Q&A` |
| **历史代号未清理** | 重写需求后留早期代号引用没删 | 改名时全文 grep 同步；早期代号留到 `_历史参照_` 段，正文一律用新词 |

**强约束**：

- 任何 `C1/C3/C4` / `A1/A2` / `D1/D2` / `Q1/Q2` / `S0x/R0x/C0x` 等代号引用，**当前节没定义就必须 cross-ref 定义出处**（用 `[§x.y](#anchor)`）
- 跨节引用 `Step N` 必须带节号；同节紧邻段内可省
- 章节标题里不带未定义代号
- 评估 case id（如 `M01-lang-zh` / `S05-negative-greet`）允许在脚本输出 / dataset.json 里裸用，**写进文档**时必须紧邻一句话说明该 case 测什么

---

## 2. 文件 / 模块命名

| 类型 | 位置 | 命名 |
|---|---|---|
| Agent core helper | `src/agent/core/` | `*_manager.py` / `*_engine.py` / `*_policy.py` / `*_bus.py` / `*_loader.py` |
| 数据持久化 | `src/memory/` | `<name>.py` 含 `<Name>Store` 类（如 `UserMemoryStore`） |
| CLI 命令处理 | `src/cli/handlers.py` | `handle_<command>` 函数 |
| LLM 调用入口 | `src/llm/provider.py` | 一个 `chat(...)` 函数为主出口 |
| 评估脚本 | `tools/agent_eval/<feature>/` | `<task>.py`（如 `recall_golden.py` / `perf_eval.py`） |
| 评估报告 | `tools/agent_eval/reports/` | `<feature>-<target>-<YYYYMMDD-HHMMSS>.md`（**强制 Markdown，禁 JSON**，详 iter_2.md §4.10） |
| 单元测试 | `tests/test_<module>.py` | Test 类名 `Test<Behavior>`，方法 `test_<scenario>` |

不允许引入新的 helper / store / manager 类型时再开新文件；先尝试在已有文件添加。

---

## 3. 测试

- 用 `pytest`，markers 在 `pytest.ini` 里 deselect 默认 integration / langchain / autogpt / extended_providers — 跑 `pytest -q` 默认走 fast UT 集
- 用 `MagicMock` mock 外部依赖（LLM / DB / 文件 IO），不要在 UT 里真发 LLM call
- 评估脚本（recall / perf）放 `tools/agent_eval/`，不放 `tests/`
- 新加 feature 必须有 UT 覆盖核心路径；触发节流 / 顺序约定这类"行为约束"要有独立 Test 类锁住

---

## 4. 错误处理

- **fail-fast schema 错误**用 `RuntimeError` + 简洁可执行的修复指引（不必含完整路径或字段名，由用户根据语义自己定位）
- LLM / 网络等**外部调用**用 `try/except Exception` + log warning，不向上传播让主流程崩
- 文件 IO 缺失 / 空 / 损坏一律 **graceful**（返回 `None` 或空，不抛异常），由调用方判断是否兜底

---

## 5. 文档同步

代码 / 配置 / CLI 命令改动时同步更新：

| 改动类型 | 同步到 |
|---|---|
| 新 config 项 | `src/config.py` **+ `.env.example` + `.env`（三处必须同步，缺一不可）**；详 §5.1 |
| 新 CLI 命令 | `src/cli/ui.py` help + `src/cli/tab_complete.py` + `docs/design.md` 对应小节 CLI 表 |
| 新 feature | `docs/iter_2.md §4.9.x` 按 §4.9.0 风格 + `docs/design.md` 按 §3.0 风格 + `README.md §1.2 Agent` 一句话 bullet |
| 评估工具 | `tools/agent_eval/reports/` 落 Markdown 报告（**禁 JSON**，详 iter_2.md §4.10）；entry point 必须 `load_dotenv(override=True)` |

### 5.1 新 config 项三处同步（强制）

新增 `os.getenv("XXX", ...)` 时**必须**同步三个文件，缺一不可：

| 文件 | 内容 | 不同步的后果 |
|---|---|---|
| `src/config.py` | 定义 + 注释（按 §1.1 风格） | — |
| `.env.example` | 列出 key + 默认值 + 用途短注释 | 新用户克隆仓库后不知道有这项可调 |
| `.env` | 实际值（即便用默认值也写一行注释提示存在） | **当前运行环境漏配，跑到这段代码时取默认值而不是用户期望值**；尤其涉及 API key / 路径 / feature flag 时会静默走错分支 |

`.env` 不进 git（被 `.gitignore` 忽略），但**本地必须有这行**。AI 改完 `config.py` 必须主动检查 `.env` 是否已含对应 key；缺则补一行（值用 `.env.example` 默认值），不要等用户运行时才发现。

---

## 6. AI 决策边界（遇歧途必须 ask）

执行任务时遇到下表所列**有多条可行路径**的决策，**先用 AskQuestion 让用户拍板，不要默认替用户选** — 尤其不要默认选"看起来更保守 / 更兼容"的路径。本项目的工程偏好是**简洁 > 兼容**，不是反过来。

| 决策场景 | 默认错误倾向 | 必须问什么 |
|---|---|---|
| **向后兼容 vs 激进清理**（schema 字段 / dead code / 弃用 API） | 默认保留旧字段 / dead code 维持兼容 | "保留旧 schema + dead code 维持兼容，还是删 DB 重建 + 全清？" |
| **破坏性操作**（删 DB / 删未跟踪文件 / 改 schema / `git clean`） | 默认替用户执行 | 先列出受影响范围 + 是否可逆，让用户确认后才动 |
| **scope 蔓延**（用户问 A，是否顺手处理相关 B） | 默认顺手做完 | "B 现在也处理一下吗？还是这次只动 A？" |

**反例 ❌**：处理废弃 `advanced/prompts/` 时遇到 `chat_history.sessions.prompt_name` schema 字段，**默认选了**"保留 schema + dead code 维持向后兼容"路径，没问用户；用户实际偏好"删 DB 重建 + 全清"，导致返工。

**正例 ✅**：[§4.9.3 Step 7](../../docs/iter_2.md#493-prompt-管理-phase-13) 处理 `advanced/prompts/` 和 `advanced/skills/` 时，先用 AskQuestion 让用户在 "废弃 / 搬迁 / 保留" 3 选项里拍板，再动手。

---

## 7. AI ↔ 用户交互节奏（叠加在 `iter_2.md §4.9` Step 1-6 之上）

**分层约定 —— 不重复定义**：

- **实现流程"做什么"** → 权威在 [`iter_2.md §4.9`](../../docs/iter_2.md#49-开始实现) 开头列的 **Step 1-6**（Review / Plan / Code / Test / Eval / Doc），本文件**不重定义**
- **文档表达风格** → 权威在 [`iter_2.md §4.9.0`](../../docs/iter_2.md#490-实现文档风格)，本文件不重定义
- **AI 跟用户怎么配合做** → 本节，**只在 Step 边界叠加交互约束**（何时问 / 何时等 / 何时动手 / 何时验收）

### 7.1 按 Step 时间线的 AI 交互约束

| 时间点 | AI 必须 | 失败模式 |
|---|---|---|
| **Step 0 之前**（预备，可选） | 若 feature 涉及用户未表现熟悉度的外部框架 / 协议 / 算法（如 MCP / LangGraph / agentskills.io），先 `AskQuestion`「先讲原理 / 先扫现状 / 直接开始？」 | 默认替用户决定跳过 → 用户后面被概念劝退 |
| **Step 0 期间**（Requirements） | 按 [§4.9.0 Step 0 结构](../../docs/iter_2.md#490-实现文档风格) 4 行表（用户故事 / 验收标准 / Scope / 依赖）起草**用户视角**需求规格；先列 `iter_2.md §4.7.3` / `§4.7.2` 早期想法作"历史参照"，再独立起草现在的规格；用 `AskQuestion` 让用户审 / 改约定；**Scope 中"暂时不做" / "显式不做"项一律同步登记到 [§4.13.1](../../docs/iter_2.md#4131-deferred-backlog暂时不做) / [§4.13.2](../../docs/iter_2.md#4132-dropped永久不做)**，feature 章节正文只 cross-ref 编号、不复述详情 | 直接照抄 `§4.7.3` 出口判据当需求（已过时） / 写成实现视角"本 feature 实现 X 功能" / 验收标准写成"API 调用成功"（实现视角不可观察） / 把"暂时不做"误归"显式不做"或反之 / 在 §4.9.x 章节正文里单列"显式不做"表复述 punt 详情（应统一进 §4.13）|
| **Step 1 期间**（Review） | 自行扫码摸底，产出**现状表 + gap 列表**；**不直接给方案**；摸底要对照 Step 0 验收标准 | 边摸底边给方案 → 剥夺用户判断空间 |
| **Step 2 期间**（Plan） | 摆**决策矩阵**（锁定 / 待定 / 显式不做 三列雏形），用 `AskQuestion` 让用户**对每个决策点拍板**；AI 不预设答案、不默认选保守路径 | 默认选"看起来更兼容"的路径 → 违反本文件 §6 |
| **Step 2 → Step 3 切换**（关键卡口） | **等用户显式 OK 才进 Step 3 动代码**（沉默 ≠ 同意） | 看到沉默就开写 → 文档反推代码 / 用户失去 review 机会 |
| **Step 3 + Step 4 期间**（Code + Test） | 全程 `TodoWrite` 跟踪；每改完一个模块即跑相关 UT + `ReadLints` 0 错 | 一口气改十几个文件再统一 lint |
| **Step 4 完成时** | 给**硬证据**（如 `415 passed / 0 failed in 12s`），不擅自宣布 feature 完成 | 自己说"OK 了" → 用户没看到 lint / UT 结果 |
| **Step 5 期间**（Eval） | 评估 case 必须**对照 Step 0 验收标准逐条**生成，不允许脱钩自由发挥 | 评估指标与 Step 0 验收标准对不上 → 评估结果失去含义 |
| **Step 5 + Step 6 完成时** | 告知端到端入口（`main.py` / `chainlit` / `recall_golden`）跑法，**由用户人工验收才算关 phase**；验收同样对照 Step 0 | AI 自己 close phase → 错过端到端 bug |

### 7.2 "Step 0 之前预备" 触发判断

AI 不确定 feature 是否触发预备步时，**默认问，不默认跳**。判断信号：

- feature 引入 ≥1 个用户当前对话中未表现出熟悉度的外部框架 / 协议 / 算法
- 历史对话里用户没问过该概念，但在 README / 通用工程文档中是高频术语
- 反例：Phase 1.1-1.4 全跳过（`user_memory` / `project rules` / 引用展示都是项目内自定义概念，无外部协议依赖）

### 7.3 典范流程

[§4.9.4 引用展示 (phase 1.4)](../../docs/iter_2.md#494-引用展示-phase-14) 是首个完整走完 Step 0-6 的范例（含回填的 Step 0 需求规格 4 行表），后续 phase 照此复用。

---

## 8. 红线（违反 = 必须修正）

- ❌ 评估报告落 JSON / CSV / TSV（详 iter_2.md §4.10 "评估报告输出约定"）
- ❌ `tools/agent_eval/reports/` 下批量删除文件（详 iter_2.md §4.10 "Agent 禁止主动清理 reports/"）
- ❌ `tools/agent_eval/**` entry point 不调 `load_dotenv()`（会导致 LLM 调用 401）
- ❌ 新增 `os.getenv(...)` 只改 `src/config.py`，不同步 `.env.example` 和 `.env`（违反本文件 §5.1）
- ❌ 不问用户就默认选向后兼容路径 / 默认执行破坏性操作 / 默认 scope 蔓延（违反本文件 §6）
- ❌ 在 `iter_2.md §4.9.x` Step 0+1+2 文档落地（Requirements + Review + Plan）前直接动代码；或 Step 2 → Step 3 切换时没等用户显式 OK 就开写（违反本文件 §7）
- ❌ Step 0 需求规格直接照抄 `iter_2.md §4.7.3` 出口判据当需求（已过时早期想法，仅作历史参照）；或 Step 0 写成实现视角而非用户视角（违反本文件 §7.1 Step 0 期间约束）
- ❌ Step 0 Scope 把"暂时不做（→ §4.13.1）"误归到"显式不做（→ §4.13.2）"或反之；或写了 punt 项但**忘记同步登记**到 `iter_2.md §4.13`（违反本文件 §7.1 Step 0 期间约束 + iter_2.md §4.9.0 Step 0 结构）
- ❌ 在 `iter_2.md §4.9.x` 章节正文里单列"显式不做"表 / "实现层显式不做"段、复述 punt 项详情（违反 iter_2.md §4.9.0 "Punt 项归集"强约束 — 所有 punt 项一律进 §4.13 唯一登记）
- ❌ Step 5 评估 case 跟 Step 0 验收标准脱钩自由发挥（违反本文件 §7.1 Step 5 期间约束）
- ❌ config 注释解释设计思想 / Phase 来源（违反本文件 §1.1）
- ❌ `docs/design.md` 出现 "Phase X 完成 Y" / "本期实现" 等时效字眼（违反 design.md §3.0）
- ❌ `docs/design.md` 插代码块（违反 design.md §3.0，行内 ` `` ` 引用文件 / 类除外）
- ❌ `docs/iter_2.md §4.9.x` 写业界对比 / 方案选型推理这种过程描述（违反 iter_2.md §4.9.0）

---

## 9. 不在本文件管的事

- API key / 密钥 → 走 `.env`，永不进 git
- LLM provider 切换 → 改 `.env` 的 `LLM_PROVIDER`，业务代码零改动
- Agent 内核切换（PYTHON / LANGCHAIN / AUTOGPT）→ 改 `.env` 的 `IMP_METHOD`
