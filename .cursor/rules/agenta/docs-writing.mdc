---
description: AgentA 文档写作规范，需要输出为文档时使用。
globs: docs/**/*.md
alwaysApply: false
---

# 文档写作规范

## 核心原则：没渲染也要好读

文档常在编辑器、git diff、终端 grep 里阅读，不能假设读者一定在看渲染后的页面。格式是辅助语义，不是装饰；正文读起来应像技术文章，而不是幻灯片备注。

- 读者不一定按你写文档的顺序看，跳进任意一节都该能读懂。任何心照不宣的代号、缩写都要给出处。
- 文档内的引用必须给出明确出处，便于溯源。
- 叙述语言遵循 conventions-core 的语言规范（中文为主、用词通俗、标题简短）。
- 写 docs/design.md 时还要额外遵守 design-doc 规则。

## 少格式，多正文

AI 写文档时容易堆格式，导致源文件噪音大、扫读费劲。默认克制；只有格式能消除歧义或承担结构职责时才用。

### 不要刻意拉表格

能通过文字说清楚的，就用段落或列表；不要默认「先拉一张表再填」。

适合用表格的场景：

- 多项并列对比：同一组维度横向对照多种方案（如 Web 入库 vs CLI 入库的能力差距）。
- 字段 / 接口 / 配置项一览：多行共享相同列名，读者需要按列检索。

不必用表格的场景：

- 讲一件事、一个根因、一段流程——写成段落或有序列表。
- 用途与工具的一一对应（「命令用代码块、流程用 Mermaid」）——几条列表即可。
- 彼此独立的多条提示，没有横向对照关系——用列表，不要硬凑「避免 | 改法」两列。

表格里同样遵守正文标准：状态列写「是 / 否 / 部分 / 未实现」，不写勾叉符号；单元格少套粗体、反引号、emoji；列不宜过多，宽表在编辑器里比段落更难读。

### 反引号

反引号不是「代码标识符高亮器」。叙述里直接写标识符即可，不要给每个文件名、函数名、环境变量名都加反引号。

| 避免 | 改为 |
|---|---|
| rag_cli ingest 灌库时，与 Web 入库共用 golden_options / golden_gen（每项都加反引号） | rag_cli ingest 灌库时，与 Web 入库共用 golden_options、golden_gen |
| 部署文档 i1.1_3_deploy.md 9.3 的 nginx 配置未设 client_max_body_size（文件名、配置项加反引号） | 部署文档 i1.1_3_deploy.md 第 9.3 节的 nginx 配置未设 client_max_body_size |
| 读原文件 web_uploads/<alias>/（路径加反引号） | 读原文件 web_uploads/<alias>/；路径含变量时可用代码块 |

#### 何时才用反引号

- 代码块里的内容（命令、配置、片段）。
- 行内短代码，且与周围中文混在一起会误读（如 POST /api/kb/upload）。
- 表格专列「字段名 / 配置项 / API 路径」时，该列可统一用反引号。

### 不要用 emoji 当标点

emoji 在纯文本里占宽度、语义含糊，禁止用于状态、优先级、章节装饰。对比表、差距表用文字状态，不用符号画脸谱。

| 避免 | 改为 |
|---|---|
| L2 ✨ | L2（待做）/ L2 目标 |
| 勾号 / 叉号表示能力有无 | 是 / 否 / 已有 / 未实现 / 有差距 |
| 火焰、警告符号当小标题 | 用正文或「注意：」起句 |

### 粗体不是标题

粗体在源文件里同样扎眼，不能代替标题层级，也不能当强调快捷键。

| 避免 | 改为 |
|---|---|
| 「目标」加粗后跟冒号 | #### 目标 + 正文；或直接写「本节目的：……」 |
| nginx、client_max_body_size、1 MB 多处加粗 | nginx 的 client_max_body_size 默认 1 MB |
| 句中多个词加粗强调 | 让句式本身表达重点；全文粗体宜极少（例如一处不可逆警告） |

小节结构用 ## / ### / ####，不要用「标签 + 粗体 + 冒号」伪造小标题。

### 其他装饰性格式

- 斜体强调：源文件里与正文无差别或更难读；直接写清楚。
- 多层嵌套引用：缩进深、diff 难看；并入正文。
- 全大写强调：像喊叫；用措辞表达强度。
- 小节之间频繁分隔线：标题已分段，不必再画线。
- 裸 URL 当链接文字：写清「部署文档 9.3 节」等可读锚文本。
- 中英之间多余空格：Web 入库、HTTP 413 即可，不要 Web  入库。

## 格式该用在哪里

- 命令、配置、日志片段：围栏代码块。
- 字段 / 接口 / 配置项一览：表格，且须满足上一节「多项并列」条件。
- 模块关系、流程分支：Mermaid 图（见 design-doc）。
- 章节与层次：Markdown 标题，不靠粗体。
- 术语首次出现：中文（英文全称）行内说明，见 conventions-core。

## 写完后自检

提交或定稿前，用纯文本扫一眼（关掉预览）：

1. 是否满屏反引号、粗体、勾叉 emoji？若是，删掉一半以上通常仍说得清。
2. 是否有本可用两三句话或列表讲清、却硬拉的表？若是，改回段落。
3. 任意抽一段，遮住标题——是否仍能分辨在讲什么？
4. 把文档贴进 terminal 或发邮件预览，是否依然可读？

任一条不过，先减格式、加句子，再考虑补结构。
