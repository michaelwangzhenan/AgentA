---
description: AgentA 文档与代码注释写作规范 — 写 docs、改注释、补 docstring 时使用。
globs: docs/**/*.md, src/**/*.py
alwaysApply: false
---

# 文档与注释写作规范

本规范适用：Markdown 文档（docs/ 等）与代码注释（# 行注释、块注释、docstring）。叙述语言另见 conventions-core；写 docs/design.md 时还要额外遵守 design-doc 规则。

## 适用范围速查

| 条目 | 文档 | 注释 |
|---|---|---|
| 没渲染也要好读 | 适用 | 适用 |
| 少格式、多正文 | 适用 | 适用 |
| 反引号 / 粗体 | 文档克制使用 | 禁止 |
| 表格 / Mermaid | 适用 | 不适用 |
| 不用 emoji 当标点 | 适用 | 适用 |
| 注释写什么、不写什么 | 不适用 | 适用 |

## 没渲染也要好读

文档与注释常在编辑器、git diff、终端 grep 里阅读，不能假设读者一定在看渲染后的页面。格式是辅助语义，不是装饰；读起来应像技术文章，而不是幻灯片备注。

- 读者不一定按你写的顺序看，跳进任意一段都该能读懂。任何心照不宣的代号、缩写都要给出处。
- 文档内的引用必须给出明确出处，便于溯源。
- 叙述语言遵循 conventions-core 的语言规范（中文为主、用词通俗、标题简短）。
- 注释与正文代码同屏出现：一句说清「为什么」，不要复述「做了什么」（代码本身应能说明做什么）。

## 少格式，多正文

AI 写作时容易堆格式，导致源文件噪音大、扫读费劲。默认克制；只有格式能消除歧义或承担结构职责时才用。

### 不要刻意拉表格

能通过文字说清楚的，就用段落或列表；不要默认「先拉一张表再填」。

适合用表格的场景（仅文档）：

- 多项并列对比：同一组维度横向对照多种方案（如 Web 入库 vs CLI 入库的能力差距）。
- 字段 / 接口 / 配置项一览：多行共享相同列名，读者需要按列检索。

不必用表格的场景：

- 讲一件事、一个根因、一段流程——写成段落或有序列表。
- 用途与工具的一一对应（「命令用代码块、流程用 Mermaid」）——几条列表即可。
- 彼此独立的多条提示，没有横向对照关系——用列表，不要硬凑「避免 | 改法」两列。

表格里同样遵守正文标准：状态列写「是 / 否 / 部分 / 未实现」，不写勾叉符号；单元格少套粗体、反引号、emoji；列不宜过多，宽表在编辑器里比段落更难读。

### 反引号

反引号不是「代码标识符高亮器」。叙述里直接写标识符即可，不要给每个文件名、函数名、环境变量名都加反引号。注释中禁止使用反引号——无渲染效果，只是噪音。

| 避免 | 改为 |
|---|---|
| rag_cli ingest 灌库时，与 Web 入库共用 golden_options / golden_gen（每项都加反引号） | rag_cli ingest 灌库时，与 Web 入库共用 golden_options、golden_gen |
| 部署文档 i1.1_3_deploy.md 9.3 的 nginx 配置未设 client_max_body_size（文件名、配置项加反引号） | 部署文档 i1.1_3_deploy.md 第 9.3 节的 nginx 配置未设 client_max_body_size |
| 读原文件 web_uploads/<alias>/（路径加反引号） | 读原文件 web_uploads/<alias>/；路径含变量时可用代码块 |

#### 何时才用反引号（仅文档）

- 代码块里的内容（命令、配置、片段）。
- 行内短代码，且与周围中文混在一起会误读（如 POST /api/kb/upload）。
- 表格专列「字段名 / 配置项 / API 路径」时，该列可统一用反引号。

### 不要用 emoji 当标点

emoji 在纯文本里占宽度、语义含糊，禁止用于状态、优先级、章节装饰。对比表、差距表用文字状态，不用符号画脸谱。注释里同样禁止用 emoji 标优先级、待办、警告。

| 避免 | 改为 |
|---|---|
| L2 ✨ | L2（待做）/ L2 目标 |
| 勾号 / 叉号表示能力有无 | 是 / 否 / 已有 / 未实现 / 有差距 |
| 火焰、警告符号当小标题 | 用正文或「注意：」起句 |
| `# TODO 🔥 紧急修复` | `# TODO：修复 xxx（见 issue #123）` |

### 粗体不是标题

粗体在源文件里同样扎眼，不能代替标题层级，也不能当强调快捷键。注释中禁止使用粗体或类似强调标记（星号、下划线包裹文字）。

| 避免 | 改为 |
|---|---|
| 「目标」加粗后跟冒号 | #### 目标 + 正文；或直接写「本节目的：……」 |
| nginx、client_max_body_size、1 MB 多处加粗 | nginx 的 client_max_body_size 默认 1 MB |
| 句中多个词加粗强调 | 让句式本身表达重点；全文粗体宜极少（例如一处不可逆警告） |

小节结构用 ## / ### / ####，不要用「标签 + 粗体 + 冒号」伪造小标题（仅文档）。

### 其他装饰格式

- 斜体强调：源文件里与正文无差别或更难读；直接写清楚。
- 多层嵌套引用：缩进深、diff 难看；并入正文。
- 全大写强调：像喊叫；用措辞表达强度。
- 小节之间频繁分隔线：标题已分段，不必再画线。
- 裸 URL 当链接文字：写清「部署文档 9.3 节」等可读锚文本。
- 中英之间多余空格：Web 入库、HTTP 413 即可，不要 Web  入库。

## 代码注释

注释遵循与文档相同的核心原则（好读、少格式、中文通俗），但不用 Markdown：纯文本句子即可。

### 写什么

- 非显而易见的业务规则：边界条件、隐式约定、与直觉相反的取舍。
- 跨模块约束：调用方必须满足的前置条件、与外部系统的时序要求。
- 临时例外：为何此处不能沿用通用写法；最好附带 issue / 任务编号或预计移除条件。
- 复杂算法或状态机：用一两句话点明不变量或阶段划分，细节留给代码。

### 不写什么

- 复述函数名、参数名、返回值能直接看出的信息（如「# 返回用户 ID」这类噪音）。
- 把注释当变更日志（如「# 2024-03-01 张三修改」）——用 git history。
- 大段被注释掉的死代码；该删就删，该用版本管理就用版本管理。
- 用注释掩盖命名不清：优先改标识符，再考虑补一句注释。

### 写法

- 行注释：短句一行；需要多条时用连续 # 行。禁止 Markdown 语法：不用反引号、粗体、列表符号。
- docstring：模块 / 公开函数写清用途与关键约束；内部辅助函数只在行为不直观时写。首行摘要 + 必要时空行后补细节，不要堆参数表除非 API 确实复杂。同样禁止反引号、粗体等 Markdown 语法。
- 中英：与 conventions-core 一致——叙述用中文，标识符保持代码里的英文拼写。
- TODO / FIXME：以「# TODO：动词开头说明待办，最好有追踪号」为范本；避免无上下文的 TODO 裸标签。

### 注释示例

```python
# 差：复述代码
def get_user_id(session):  # 从 session 获取 user_id
    return session.user_id

# 好：说明非显而易见的原因
# 必须在 flush 之前注册 listener，否则首批事件会丢
def _bind_listener(store):
    ...

# 差：emoji + 含糊
# FIXME 🔥 这里有问题

# 差：Markdown 格式（反引号、粗体）
# 必须设置 `client_max_body_size`，否则 **上传失败**

# 好：可行动
# FIXME：并发下可能重复入库，见 backlog #42
```

## 文档格式用法

- 命令、配置、日志片段：围栏代码块。
- 字段 / 接口 / 配置项一览：表格，且须满足上一节「多项并列」条件。
- 模块关系、流程分支：Mermaid 图（见 design-doc）。
- 章节与层次：Markdown 标题，不靠粗体。
- 术语首次出现：中文（英文全称）行内说明，见 conventions-core。

## 写完后自检

提交或定稿前，用纯文本扫一眼（关掉预览 / 折叠语法高亮）：

1. 是否满屏反引号、粗体、勾叉 emoji（文档）或反引号、粗体、emoji、无意义 TODO（注释）？若是，删掉一半以上通常仍说得清。
2. 是否有本可用两三句话或列表讲清、却硬拉的表？若是，改回段落。
3. 任意抽一段注释或一节文档，遮住标题 / 函数名——是否仍能分辨在讲什么？
4. 注释是否在复述代码？若是，删注释或改代码命名。
5. 把内容贴进 terminal 或发邮件预览，是否依然可读？

任一条不过，先减格式、加句子，再考虑补结构。
