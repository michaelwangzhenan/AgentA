# 项目偏好（示例）

把这个文件 `cp` 一份为 `.agenta/rules.md`，Agent 启动时会自动读取并注入到 system prompt。
改完后**重启进程**生效（不做热加载）。

下面是一些常见写法示例，按自己需要保留 / 删除 / 改写：

## 语言
- 始终用中文回答用户的问题，即使用户用英文提问。

## 格式
- 回答用流畅的段落散文，不要使用 bullet points 或编号列表。
- 不要用 em dash（——），改用顿号、逗号或句号。

## 引用风格
- 引用论文或文档时必须带具体页码或章节号（如 `p.12` 或 `§3.2`），方便溯源。

## 背景与术语
- 我是 5G 通信协议工程师，回答可以直接使用 PDCP / RLC / MAC 等专业缩写，不需要先科普概念。
