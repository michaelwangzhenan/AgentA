！！！先讨论，我让你开始实现，才开始实现！！

# 1. 背景
VPS 已购买，域名已购买，ICP备案已申请。
后面 agenta.xin 将对外开发，需要将其打造成企业级实用网站。
当前的知识库里都是前期测试实用的临时数据，需要清空重建。

# 2. 目标

## 2.1. 范围
构造完整且有意义的知识库，让用户可以做有意义的业务问答。
本阶段不增加新功能，但需要优化入库过程中碰到的问题。

## 2.2. 知识库两个主题
1. 我的个人信息：我的能力，简历信息，项目信息，我的故事。
   后面找工作时， HR/猎头可以来 agenta.xin 直接对话以获得我的信息。
   这一步一个重要的事情是，要信息脱敏，不能把我的私有信息/底牌给入库了
2. 3GPP 标准文档 + 脱敏的项目知识，体现我的通信专业方向

## 2.3. golden 集
生成真正有效的 golden 集。


# 3. 实施
## 3.1. 清空现有 KB

执行步骤

```bash
# 1. 停服务

# 2. 全量清空向量 + BM25 + sidecar（需输入 yes 确认）
.venv/bin/python -m tools.cli.rag_cli clear

# 3. Golden 整库删除
rm db/sqlite/rag_golden.db

# 4. 扫一遍 web_uploads 各子目录
rm -rf datasets/web_uploads/*

# 5. 重启服务后验收
.venv/bin/python -m tools.cli.rag_cli status   # 各 collection 应为 0 或不存在
```


## 3.2. 入库优化
### 3.2.1. 默认入库模型设为 api-m3

UI 入库下拉默认选中 `api-m3`（跟随 `EMBEDDING_MODEL`）。

- `GET /api/kb/collections` 新增 `default_ingest_alias` 字段
- 前端入库面板用该字段初始化下拉，不再从 `is_default` 库的 alias 推断（api-m3 时否则会落到 m3）
- `.env` / `.env.example`：`EMBEDDING_MODEL=api-m3`

### 3.2.2. 入库时生成 golden 的LLM 可选

1. 在入库目标库下拉旁加 「评估题 LLM」，默认：不生成，可选： kimi2.5 和 DeepSeek V4 Flash
2. EVAL_AUTO_GOLDEN_ENABLED  改成 EVAL_GOLDEN_LLM，对应上面3个可选值，可在 3.2.3 复用
3. 


### 3.2.3. 生成 golden 可选 LLM
知识库 L2 点「生成评估」按钮时，增加可选生成 golden 的 LLM。
只加 kimi2.5 和 DeepSeek V4 Flash

### 3.2.4. golden 集讨论
golden 集的范围和数量
数据结构

### 3.2.5. 优化 LLM 生成 golden集合
Review 现有生成方式，看看如何让 LLM 生成的 golden 更有效。
大文件/小文件的策略应该不一样
生成多少也需要考虑

### 3.2.6. CLI 对齐


## 3.3. 准备材料


## 3.4. 入库 + golden 集生成


# 4. 评估与优化
