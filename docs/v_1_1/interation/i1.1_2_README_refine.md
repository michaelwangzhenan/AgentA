# 1. 背景
V1.0 结束时没有更新README，内容和实现已有很大差距


# 2. 支持材料
1. docs/ 下的开发文档
2. 源代码


# 3. 本期任务
1. 重新设计 README badge 和章节结构
2. 逐章节打磨内容：先对齐代码实现和文档，再写入对应章节


# 4. 结构设计
## 4.1. README 的读者

本项目本质是**个人作品集**，不追求做成活跃开源产品。读者优先级：

| 优先级 | 读者 | 为什么来 | 最关心 | 停留 |
|---|---|---|---|---|
| **第一** | 招聘方 / 面试官 | 从简历 / GitHub 链接过来，评估作者水平 | 项目难度、工程素养、亮点是否真实 | 30 秒扫，决定要不要细看 |
| 第二 | 技术同行 / 学习者 | 搜 RAG / Agent 实现，想参考思路 | 架构怎么设计、哪些是原创、能不能看懂 | 几分钟，会点进 `design.md` |
| 次要 | 潜在使用者 | 想跑起来当私有知识库用 | 怎么装、怎么配、能不能跑通 | 到"快速开始"即够 |

**定调**：整体调性和信息排序服务于第一读者（招聘方/面试官）；技术同行靠"架构 + 文档导读"承接。使用者与未来维护者不单列，靠"快速开始 / 文档导读 / 命令区"自然兼顾，不为其重构结构。

## 4.2. README 要展示什么

**判定标准**：能帮招聘方在 30 秒内建立"难度 + 亮点 + 真实性 + 工程素养"印象的 → 必须展示且靠前；纯操作细节（怎么配、怎么跑）→ 使用者向，保留但折叠/靠后。

| 类别 | 内容 | 处理 |
|---|---|---|
| 必须·靠前 | 一句话定位（从零实现 · 本地私有知识库 Agent） | 顶部第一屏 |
| 必须·靠前 | 演示截图 / GIF | 真实性证据，比文字有说服力 |
| 必须·靠前 | 核心亮点扫读段 | 顶部先给一段/一行式亮点，供 30 秒扫读 |
| 必须 | 核心能力详细表格 | 承接扫读段，靠后展开 |
| 必须·靠前 | 架构图 | 一眼看出设计能力 |
| 必须 | 工程化质量（测试 / 评估 / CI 三道门） | 区分"跑通 demo"与"生产级素养"，招聘方尤其看重 |
| 保留·靠后 | 快速开始（装 / 配 / 启动） | 使用者向 |
| 去掉 | 主要配置（.env 大块） | 与 `.env.example` 冗余；快速开始里一句"配置见 `.env.example`"带过 |
| 保留·折叠 | 实用工具命令区 | `<details>` 折叠，侧面证明有配套工具链 |
| 保留·强调 | 文档导读 | 承接技术同行；突出"8 轮迭代 + `design.md`"作为深度设计信号 |

**去掉**：原"项目数据一览"（代码量 / 文档行数 / 测试数等量化表）。理由——态势数字易过时，且专门堆一张"数据表"显刻意；量化信息改为分散融进亮点段与工程化质量节里自然呈现。

**叙述口吻（重要）**：README 里所有能力/亮点写的都是 **AgentA（产品）能做什么**，不直接写作者能力。招聘方是通过产品的难度反推作者水平——产品能力是作者能力的证据。因此统一用**纯产品视角客观陈述**（"AgentA 支持 X / 集成 Y"），靠内容本身难度说话，不用"我实现了…""从零手写"这类自夸口吻。

## 4.3. 目标结构

**排布逻辑**：倒金字塔——最能打动招聘方的（定位 / 演示 / 亮点 / 架构）全放前半屏，操作细节沉底。

| # | 章节 | 内容 | 服务读者 |
|---|---|---|---|
| — | 标题 + badges | 项目名 + 状态徽章 | 全部 |
| 1 | 一句话定位 + 演示 | 定位句 + 截图 / GIF | 招聘方（30 秒建立"是什么 + 真实"） |
| 2 | 核心亮点扫读段 | 一段 / bullet 式硬核点罗列 | 招聘方（扫读） |
| 3 | 整体架构图 | mermaid 架构图 | 招聘方 + 同行 |
| 4 | 核心能力 | RAG / Agent / 交互 / 3 套实现详细表格 | 同行 |
| 5 | 工程化质量 | 测试 / 评估 / CI 三道门 | 招聘方（生产级素养） |
| 6 | 快速开始 | 装 / 配（一句带 `.env.example`）/ 启动 | 使用者 |
| 7 | 实用工具（折叠） | rag_cli / eval 命令 | 使用者 / 维护者 |
| 8 | 文档导读（强调） | 8 轮迭代 + `design.md` 引流 | 同行（深度信号） |

**badge 设计**：去掉计数型徽章（Tests 46 files、Code 27K LOC 等）——与"去掉数据一览"同理，态势数字易过时。只留稳定徽章：CI 状态 / Python 版本 / License 等。

# 5. 实施记录

按目标结构逐章节核对代码实现后落地，主要过时点与改动：

| 章节 | 发现的过时点 | 改动 |
|---|---|---|
| 定位句 | provider 9 个（含豆包） | 实际 10 个（新增 MiniMax / Gemini，移除豆包）；WebUI 改统称 |
| 亮点段 | 原"项目数据一览"数字过时 | 去掉，替换为 6 条亮点扫读段 |
| 架构图 | 无（与 design.md 一致） | 不改 |
| 核心能力 | WebUI=chainlit、SDK 标 TBD、provider 9 | WebUI 改 React+FastAPI（`src.api.run` + `npm run dev`）；SDK 去 TBD；provider 改 10 |
| 工程化质量 | 46 测试文件、marker `fast/ext/int`、报告路径 `*_eval/reports/`、CI 2 步 | 改 87 文件；marker 实际 `integration/slow/langchain/autogpt`；路径 `tools/reports/<eval>/`；CI 3 job（+ 非 LLM 评估门禁） |
| 快速开始 | chainlit 启动命令 | 改 React+FastAPI 启动；删掉"主要配置"整节（与 `.env.example` 冗余） |
| 实用工具 | golden.json、旧报告路径、`-fast` 旧提示 | golden 改 `rag_golden.db`；路径改 `tools/reports/`；整节 `<details>` 折叠 |
| 文档导读 | `docs/iter_*.md` 全部失效、只列 8 篇 | 迭代文档已移至 `docs/v_1_0/iteration/`（20+ 篇）；改精选式，按目录汇总并强调深度 |

**自主决策（非破坏性，记录备查）**：

- badge 无 LICENSE 文件，未加 License 徽章；补 FastAPI / React / TypeScript 稳定徽章强化全栈信号
- SDK 去掉 TBD：`AgentAPI` 实为可用的程序化入口
- 标题编号：README 全部标题去掉编号，只留文字（符合 README 惯例，GitHub 自带 TOC，避免重排漂移）；编号风格留给 `design.md` 这类技术文档。仓库无 `.vscode/settings.json`，编号是 Markdown All in One 手动命令所加、不会自动回填
- 迭代篇数用"20+ 篇"稳妥表述（V1.0 迭代实际 22 篇）

## 5.1. 深度对照复核（三代理并行核对 docs/ + 源码）

在初稿基础上又对 README 全部论断做了一轮"逐条对照源码 + `design.md`"的复核，修正 8 处失准表述：

| # | 原表述（夸大/说岔） | 改为（对照代码） | 证据 |
|---|---|---|---|
| 1 | 多模型 Embedding「自动按语种路由」 | 按别名分库；检索按 `RAG_ACTIVE_EMBEDDINGS` 多库并行召回再融合 | `retriever.py` 不按 query 语种选库 |
| 2 | 二阶段精排「按 per-model 阈值过滤」 | per-model 阈值属 **Dense 阶段**；精排后是可选全局阈值 | `RAG_DENSE_MIN_SCORE_PER_MODEL` vs `RAG_RERANK_MIN_SCORE` |
| 3 | 「测验批改 / RAG 召回」都双重复核 | 双重复核仅测验批改；RAG 召回是单层 Critic 过滤 | `critic_manager` Q1 双阶段 / R1 单阶段 |
| 4 | Context 第 4 层「工具结果/用户输入」 | 第 4 层只有 `<active_study_plan>`；工具结果/用户输入在 messages | `agent_commons.build_layered_system_prompt` |
| 5 | Thinking「Budget / Adaptive 两策略」 | **Adaptive 已不存在**，只有固定预算 tokens | `thinking_policy.effective_budget()` 直接返回固定值 |
| 6 | EventBus 事件名 `tool_result/message` | 实际无此名，改为中文类别描述 | `event_bus.py` 实为 `token_chunk`/`tool_call_start` 等 |
| 7 | 学习计划「可跨 session 注入 prompt」 | 跨 session 持久化，注入需当前会话 `/study load`（仅 CLI） | `agent_commons.build_active_study_plan_block` |
| 8 | 出题「MCQ（多选题）+ 简答」 | 实际单选 / 多选 / 简答三类 | `quiz_store` `mcq_single/mcq_multi/short_answer` |

**新增能力入 README（用户确认补 4 项）**：模型路由 `model_router`、语义缓存 `SemanticCacheStore`、深度研究 `research_engine`（进 Agent 能力表）；用量统计 / 备份恢复 / 质量看板 / 权限管理（进核心特性"配套 Web 管理后台"一条）。

**决策记录**：
- `design.md` 也有过时点（Thinking Adaptive 三档 @ `design.md:1387`），本期只改 README，design.md 留待单独一次校对（用户拍板）。
- 复核确认一致、无需改的：RRF 混合检索、七格式+OCR、`[n]`溯源、安全五道防线、MCP fetch+filesystem、三套实现、SM-2 SRS、CLI/Web 各能力、`rag_golden.db`、快速开始命令。

**待用户补**：演示区截图 / GIF（CLI 流式 + 新 React Web UI），建议存 `docs/assets/`。
