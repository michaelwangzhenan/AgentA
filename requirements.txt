# Vector database
chromadb

# Embedding model (local, no API needed)
sentence-transformers

# LLM client (OpenAI-compatible: Kimi / OpenAI / DeepSeek / Grok / Ollama)
openai

# Anthropic Claude SDK (used in Phase 7)
anthropic

# HTTP client (used directly for proxy injection in provider.py)
httpx

# CLI Tab completion
prompt_toolkit

# Document parsers
pypdf
python-docx
python-pptx
openpyxl
beautifulsoup4
lxml

# Iter-4 可选：增强 PDF 解析
# pymupdf 文本/表格质量优于 pypdf，且能渲染图片喂给 OCR；
# rapidocr-onnxruntime 在扫描版/图片版 PDF 字符密度过低时自动兜底（CPU only / 中英）。
# 不安装时 parser 自动退回 pypdf，且 RAG_OCR_FALLBACK_ENABLED 静默禁用。
# pymupdf
# pdfplumber
# rapidocr-onnxruntime

# Web scraping
requests

# YAML parsing (for Skills SKILL.md frontmatter)
pyyaml

# Environment variables
python-dotenv


# LangChain integration (Phase 9)
langchain-core
langchain
langchain-openai
langchain-anthropic

# MCP (Model Context Protocol) host SDK（src/agent/core/mcp_manager.py）
mcp

# Testing
pytest
