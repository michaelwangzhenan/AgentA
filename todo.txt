ToDo List：
1. 对话记忆 => Done
  目标： 将 messages 历史持久化到 SQLite，重启后仍可延续上下文，支持多轮跨会话对话

2. 知识库未命中时自动联网搜索 => Done
  目标： 当 `search_knowledge` 返回空或相似度过低时，通过指令（对话）触发 LLM 自行搜索，
    并提示 LLM 优先在国内网站查找信息，而非直接返回"暂无内容"。

3. 重排序（Reranking）=> Done
  目标： 在向量检索结果上加 Cross-Encoder 二次排序，提升精度
  背景:
    当前 retriever.py 采用两个 Bi-Encoder（SentenceTransformer）+ round-robin 合并：
      · all-MiniLM-L6-v2     → 英文向量化入库 / 召回 kb_en collection
      · BAAI/bge-small-zh    → 中文向量化入库 / 召回 kb_zh collection
    Bi-Encoder 速度快但精度有限——它将 query 和 document 各自单独编码，
    无法捕捉两者之间的细粒度语义交互。

    Reranking 引入第三个模型 Cross-Encoder（ms-marco-MiniLM-L-6-v2）作为二阶段精排：
      · 三个模型职责不同，互不替代，同时工作：
          阶段一（召回）：两个 Bi-Encoder 各自查询 → round-robin 合并取 top_k×3 候选
          阶段二（精排）：Cross-Encoder 对候选逐对打分 → 降序重排 → 截取最终 top_k 条
      · RERANKER_ENABLED=false 时跳过阶段二，两个 Bi-Encoder 行为完全不变（向后兼容）

    三个模型对比：
      模型                       职责          输入          特点
      all-MiniLM-L6-v2          英文召回       单个文本      速度快，支持批量入库
      BAAI/bge-small-zh         中文召回       单个文本      速度快，支持批量入库
      ms-marco-MiniLM-L-6-v2    精排打分       (query, doc)  精度高，不可用于入库

4. 定制 Prompt => Done
  目标：
    - 支持配置文件(./advanced/prompts/prompt_name.prompt.md)， prompt_name.prompt.md 为用户自定义 prompt.
    - 该 prompt 覆盖 Agent 的系统提示(SYSTEM_PROMPT)，适配不同业务场景（5G专家、代码助手等）。
    - 系统启动时检查./advanced/prompts目录，把目录下的prompt 添加为可用命令，只添加一次，不要重复
    - 检查./advanced/prompts目录与可用命令的一致性，删除掉的prompt对应的命令也要删除
    - 用法：用户输入 "/prompt_name [问题（可选）]""
    - 该功能类似 github copilot 的配置 .github/prompts/prompt_name.prompt.md

5. 支持 Skills => Done
  目标： 
    符合 skills 规范， 参考: https://agentskills.io/specification
    如何支持 skills 参考: https://agentskills.io/client-implementation/adding-skills-support
    本项目 skills 目录： advanced/skills/


6. 支持 MCP
  目标： 
    支持MCP最新版标准： https://modelcontextprotocol.io/specification/2025-11-25
    支持接入 local mcp server 来访问本地文件系统和工具 : https://modelcontextprotocol.io/docs/develop/connect-local-servers
    支持 MCP 最新版标准（2025-11-25）：接入 local/remote MCP Server，将其工具集无缝融入 Agent ReAct 循环。
    对外表现：MCP 工具与本地工具（search_knowledge/fetch_url）无差别，LLM 通过同一套 function calling 调用。

  【设计原则】
    · 向后兼容：MCP_ENABLED=false 时现有行为零变化，不引入额外依赖加载。
    · 无侵入：不改造 Agent ReAct 核心循环逻辑，仅扩展 tools 来源与 execute_tool 路由。
    · 配置驱动：通过 YAML 声明式配置 MCP Server（stdio / SSE），支持热重载。

  【模块设计】src/mcp/
    1. config.py —— 配置解析
       · 读取 advanced/mcp/servers.yaml（或 .env 中 MCP_SERVERS_CONFIG 指定路径）。
       · 单服务器字段：name, transport(stdio|sse), command/args(env 可注入), url, timeout。
       · 环境变量占位符支持（如 ${BING_API_KEY}），在解析时替换。

    2. client.py —— MCP 客户端封装
       · 基于官方 mcp Python SDK（pip install mcp）。
       · 统一抽象：McpClient 封装 stdio 子进程（StdioServerParameters + ClientSession）
         或 SSE 长连接（HttpMcpTransport）。
       · 暴露同步化 API：list_tools()、call_tool(name, arguments) → 内部用 asyncio.run 或
         后台事件循环包裹异步 SDK 接口，避免重构整个项目为 async。
       · 生命周期管理：connect() / disconnect() / health_check()；进程退出时自动清理子进程。

    3. adapter.py —— 格式适配器
       · mcp_tool_to_openai(mcp_tool) → OpenAI Function Calling JSON Schema。
       · 名称隔离：MCP 工具名加前缀 "mcp_{server_name}_{tool_name}"，防止与本地工具冲突。
       · 反向映射：维护 {prefixed_name: (server_name, original_name)}，供 execute_tool 路由。

    4. manager.py —— 多服务器聚合管理
       · MCPManager 持有 {server_name: McpClient} 字典。
       · aggregate_tools() → 收集所有 server 的 tools，经 adapter 转换后统一返回列表。
       · dispatch_tool(prefixed_name, args) → 解析前缀，路由到对应 client.call_tool()。
       · 启动时并行连接所有 server，失败时记录 warning 但不阻断主程序（某 server 挂了仅该 server 工具不可用）。

  【现有代码改造点】
    1. requirements.txt：新增 mcp（官方 Python SDK）。
    2. src/config.py：
       · MCP_ENABLED: bool（默认 false）
       · MCP_SERVERS_CONFIG: str（默认 advanced/mcp/servers.yaml）
       · MCP_TOOL_TIMEOUT: int（默认 30s）
    3. src/agent/tools.py：
       · get_tools(skill_bodies, mcp_tools=None) → 若传入 mcp_tools，追加到 tools 列表尾部。
       · execute_tool(name, args, skill_bodies, mcp_manager=None) → match case 增加 "mcp_*" 分支，
         调用 mcp_manager.dispatch_tool()；ToolResult 包装保持不变。
    4. src/agent/agent.py：
       · Agent.__init__ 新增可选参数 mcp_manager: MCPManager | None。
       · run() 中：active_tools = get_tools(self._skill_bodies, self._mcp_manager.aggregate_tools() if self._mcp_manager else None)。
    5. src/cli/handlers.py & main.py：
       · main() 启动时：若 MCP_ENABLED，初始化 MCPManager 并 connect_all()；传入 make_agent。
       · 新增 /mcp 命令：
         /mcp list    — 列出各 server 状态及可用工具
         /mcp reload  — 断开并重新读取配置、重连 server
       · make_agent() 签名增加 mcp_manager 参数并透传给 Agent。
    6. advanced/mcp/ 目录：
       · .gitkeep + servers.yaml.example（示例配置本地 filesystem mcp server）。

  【风险与应对】
    · 异步冲突：mcp SDK 重度依赖 asyncio，而项目整体为同步代码。
      应对：在 McpClient 内部隔离事件循环（专用线程 + asyncio.new_event_loop() 或
      asyncio.run() 包裹单条调用），对外保持同步接口。
    · 工具名冲突：多个 MCP server 可能出现同名工具。
      应对：adapter.py 强制加 "mcp_{server_name}_" 前缀；LLM 侧通过 description 区分语义。
    · 超时/死锁：local stdio server（如 filesystem）可能挂起。
      应对：call_tool 增加 timeout（信号或 concurrent.futures.ThreadPoolExecutor）；
      超时时返回 ToolResult(status="error", content="MCP tool timeout")。
    · 上下文污染：MCP 工具返回内容过大挤占 LLM 上下文。
      应对：参考现有 fetch_url max_chars 截断策略，在 adapter 或 manager 层统一限制 MCP 结果长度。

  【测试】tests/test_mcp.py
    · Mock McpClient（不启动真实子进程），测试 adapter 格式转换正确性。
    · 测试 execute_tool 路由：传入 mcp_ 前缀名时正确调用 manager.dispatch_tool。
    · 测试 MCPManager 聚合：多 server 同名工具前缀隔离。
    · 测试配置解析：环境变量注入、缺字段容错。
    · 集成测试（optional）：启动官方 filesystem mcp server，测试 list_directory 工具端到端。

  【实现顺序】
    ① 引入 mcp SDK，搭好 src/mcp/ 目录骨架（config + adapter + client stub）。
    ② 实现 adapter 与单 client stdio 连接，对接本地 filesystem mcp server 跑通单工具调用。
    ③ 实现 manager 聚合与 agent/tools.py 改造，使 MCP 工具参与 ReAct 循环。
    ④ 添加 CLI /mcp 命令与热重载。
    ⑤ 补全 tests/test_mcp.py，更新 .env.example 与文档。


7. 用户自定义 Workflow
  目标： 支持用户通过配置文件或脚本（如 YAML/DSL）自定义工作流，编排多步骤工具调用和 LLM 调用序列，
    实现无需编写代码即可完成复杂自动化任务。
    注：与 no.5「定制 Skills」的边界 —— Skills 管理的是单个工具的描述和参数；Workflow 管理的是
    多个工具/LLM 的调用顺序和条件分支，属于更高层的编排逻辑。前置依赖 no.5 完成后再实现。


8. 优化 ReAct => Done
  目标： 提升 Agent 在多轮对话中的推理能力和工具调用效率，具体包括：
    · 动态工具选择策略：根据问题类型优化 System Prompt，引导 LLM 更准确地决策何时调用哪个工具
    · 工具调用失败重试：工具执行出错时自动重试或降级，而非直接返回失败
    · 多步骤任务规划：支持 LLM 在单次 run() 中拆解复杂任务并按步骤执行
    · 工具结果质量判断：LLM 能识别工具返回内容为空/无关时主动切换策略，而非直接生成回答

9. 多Agent协作 A2A
  目标： 支持多个 Agent 协同工作，共享知识和任务，提高整体效率


10. Web UI
  目标： 接入 Gradio 或 Streamlit，提供图形界面


11. 支持企业级向量数据库
  目标： 支持接入企业级向量数据库，如 Pinecone、Weaviate 等，提高数据存储和检索能力


12. Ollama
  目标： 支持本地 Ollama 模型，完全离线运行

[x]13. URL 批量导入
  目标： 支持通过配置文件批量爬取指定网页并入库


[x]14. 文档自动同步
  目标： 用 `watchdog` 监听 `docs/` 变化，自动增量入库


[x]15. API/SDK接口
  目标： 方便第三方系统集成


16. 安全与权限管理
  目标： 适合企业/团队场景


[x]17. 多语言支持
  目标： 适应国际化需求


18. 本地工具扩展
  目标： 开发更多工具，扩充 Agent 工具集，增强处理复杂任务的能力
  --> 工具扩展后，使用工具的 prompt, skills, reAct 等要对应优化
  候选工具清单：
    a. web_search：集成 Bing Search API 或 DuckDuckGo，直接返回搜索结果摘要+URL列表，
       比 fetch_url 更高效（不需抓取全页，直接得到摘要）。可作为未来自动搜索的工具基础。
       实现方案：
         · 首选：Bing Search API（国内可访，安全密起环境变量 BING_SEARCH_API_KEY）
         · 备选：duckduckgo-search 库（无需 Key，但需代理）
    b. run_python：在沙筱环境中执行 Python 代码片段，返回输出结果
    c. read_file：读取本地文件内容（如日志、csv），支持行范围指定
    d. shell_cmd：执行本地 Shell 命令，返回 stdout/stderr


19. 使用Tab键补全命令 => Done
  CLI 输入命令时支持Tab键补
20. Agent 优化 => Done
  目标：
    提升推理准确性
    提高推理性能，考虑： 减少推理时间，CPU/内存消耗，尽量节省token(保证推理质量，寻找最佳性价比)
    Clean Code
  
21. 优化 session 信息 => Done
  目标：
    使用/session <id> 命令时，切回到原来的 prompt_name， 而不是"你"
    /session 信息中，增加一列显示对应的 prompt_name， 如"5g-expert"
    /session 切换命令中，提示 session id 只需显示前8位，后面加上"首问 "，用户更加容易选择要切换的session

22. tests 优化 => Done
  目标：
    统一 test_xxx.py 命名风格，使命名更common 和 符合命名习惯， 去除“phaseN”。
    精简合并case,只保留必要case,节省测试资源。
    重构case, 使 cases 那模块分组。
23. 统计tokens => Done
  目标： 增加在每次对话结束时，显示当前对话消耗的 token数。
24. 添加 UT 测试脚本 => Done
  脚本类型：shell
  脚本位置和名称：workspace_root/t.sh
  用户可以运行 t [parameter] 来运行对应的UT
  脚本命令 分别对应以下测试case
  t -h : 显示支持的命令
  t -all: .venv\Scripts\python -m pytest
  t -not: .venv\Scripts\python -m pytest -m "not integration" -v
  t -module_name: 分别对应下面的 case
    .venv\Scripts\python -m pytest tests/test_llm.py           # LLM 配置 & Provider
    .venv\Scripts\python -m pytest tests/test_parser.py        # 文档解析（7 种格式）
    .venv\Scripts\python -m pytest tests/test_rag.py           # 分块 & 双语检索 & Reranker
    .venv\Scripts\python -m pytest tests/test_tools.py         # 工具层（search/fetch）
    .venv\Scripts\python -m pytest tests/test_agent.py         # Agent ReAct 循环
    .venv\Scripts\python -m pytest tests/test_memory.py        # 对话记忆持久化
    .venv\Scripts\python -m pytest tests/test_prompt_loader.py # 自定义 Prompt 加载
25. 对话导出 => Done
  实现/save <文件名>命令，把当前 session 完整对话记录存储到 history/文件名.md
26. 支持 Thinking => Done
  目标：
    1. 增加 Extended Thinking 支持，是否开启用户可选
    2. 代码框架可扩展，为兼容各种LLM 抽象 common 接口,如 def call_with_thinking(provider, prompt, budget=8000)
      budget_tokens 推荐： 
      简单推理: 1,024 ~ 3,000, 
      复杂分析: 8,000 ~ 16,000
      高难度Agent 任务: 32,000+
    3. 思考过程流式输出
    注意： thinking 内容不应该被用来做 prompt injection 的载体

27. 支持跨 session 记忆 => Done
  需求：
    1. 记忆写入
      记忆提取：
        - 每次对话结束时自动触发
        - 对话中用户显式说"记住这个"时立即触发
      提取的内容：
        - 用户偏好（风格、格式、语言）
        - 用户背景（职业、技术栈、项目上下文）
        - 用户的明确指令（"不要用 bullet points"）
        - 任务进度（未完成的工作、决策记录）
        - 用户纠错记录（agent 犯过的错，避免重犯）
    2. 记忆存储
      存储格式：结构化 key-value 或带 tag 的文本条目，方便检索
      存储位置： sqlite_db/user_character.db
      版本/时间戳：每条记忆记录创建时间，支持过期或衰减
    3. 记忆读取
      触发时机：每次新 session 开始时自动加载
      加载策略：不是全量加载，而是按相关性筛选（避免 context 污染）
      注入位置：放入 context 的固定位置
      容量控制：限制注入的记忆 token 数，防止挤压有效上下文
    4. 记忆更新与删除
      用户可以显式要求更新或删除某条记忆
      支持覆盖旧记忆（同类信息去重，不重复堆积）
      支持记忆过期策略（长时间未访问的低优先级记忆可清理）
      记忆冲突时,新信息覆盖旧信息
    5. 记忆的使用
      Agent 在回答时应自然地运用记忆，而不是明说"根据我的记忆"
      记忆不应影响 agent 的客观判断（不能因为记忆说"用户喜欢 X"就盲目迎合）
      记忆内容需要做安全校验，防止用户注入恶意指令进记忆系统
    6. 用户可见性与控制
      用户可以查询"你记得关于我的什么"
      用户可以手动编辑、删除记忆
      记忆的存储和使用对用户透明

28. Adaptive Thinking => Done
  目标：
    根据问题复杂度自动估算合适的 thinking budget_tokens，替代固定值：
      · LOW    (1 500)  — 短问、简单事实类（< 25 字符，或含"什么是"等低复杂度词）
      · MEDIUM (8 000)  — 分析、解释、对比（默认档）
      · HIGH   (32 000) — 架构、规划、多步骤深度推理（含"设计/架构/规划"等词,或 > 200 字符）
  实现：
    · estimate_thinking_budget(messages, max_budget) — 在 provider.py 中
    · Agent.__init__ 新增 thinking_adaptive: bool 参数，来自 config.THINKING_ADAPTIVE
    · Agent.run() 当 thinking_adaptive=True 时调用 estimate_thinking_budget，结果传给 call_with_thinking
    · CLI：/thinking adaptive 开启；/thinking 状态显示自适应档位
    · .env.example 新增 THINKING_ADAPTIVE=false

29. 跨 session 记忆 enhancement
    记忆提取: 对话中出现重要事实时实时触发
    用户隔离：多用户场景下记忆必须按 user_id 严格隔离 --> depends on 16. 
    隐身模式：用户可以关闭记忆功能（类似 Incognito 模式）， 从而不会读取、也不会写入任何记忆的对话
    记忆压缩：当context超过总预算85% 时，压缩记忆。15% 预留给response ?

30. 支持 GraphRAG / Knowledge Graph
  不再存储原始文本，而是把文档转化为实体-关系图谱：
  文档 → 抽取实体和关系 → 知识图谱
  问题 → 图上路径搜索 → 精确答案
  核心优势： 擅长多跳推理（"A 的老板的老板是谁？"这类问题）
  局限： 构建图谱成本高，动态更新难

31. Harness 中可以实现的部分
  LLM 直接返回文本 → 输出最终回答，评价回答，决定是否继续提问 => 建立反馈机制，让 AI 能自我检查和修正。
32. 支持sandbox
33. Sub Agent
34. 结合 systemInsight/walle
35. SYSTEM_PROMPT 从文件读入
36. Arch Refine
  一个企业级 RAG 系统，数据量100万文档，如何设计索引和检索架构？
  Agent系统如何保证可靠性？工具调用失败怎么处理？
  多Agent协作的数据流如何设计，避免上下文爆炸？
  → 实践：加入3GPP文档场景，遇到真实规模问题
  → 输出：能画出完整的 AI 系统架构图并解释每个决策
37. 使用 LangChain 实现
  【现状摘要】核心在 `src/agent/agent.py` 手写 ReAct：`chat()`/`call_with_thinking()` + OpenAI 格式 tools；`src/agent/tools.py` 提供 JSON Schema 与 `execute_tool`；`src/memory/chat_history.py` 持久化每轮 user/assistant/tool；`src/llm/provider.py` 多厂商（OpenAI 兼容 / Claude / Qwen）与 Extended Thinking。
  【目标界定】先定范围：A) 仅把「模型调用 + 工具绑定」换成 LangChain，自管循环与 DB；B) 用 `create_tool_calling_agent` + `AgentExecutor`（或 LangGraph）接管循环；C) RAG/向量库也迁到 `langchain-chroma` 等。A 改动最小，B/C 收益大但测试与行为对齐成本高。
  【依赖】`requirements.txt` 增加 `langchain-core`、`langchain`（或拆 `langchain-openai` / `langchain-anthropic` 等），与现有 `openai`/`anthropic` 版本对齐；评估是否保留直连 SDK 作 fallback。
  【模型层】用 `ChatOpenAI`（`base_url`+`api_key` 映射现有 `config` 各 provider）或 `ChatAnthropic` 对应 Claude；将 `extra_body`、代理（`httpx`）等与 LC 客户端构造对齐。Extended Thinking：查 `langchain-anthropic`/厂商是否暴露 thinking；若无则保留 `call_with_thinking` 为 Runnable 包装或「非 LC 分支」。
  【工具层】把 `TOOLS`/`load_skill` 转为 `@tool` / `StructuredTool`，内部仍调用现有 `execute_tool` 与 RAG，避免重复实现抓取与 Chroma 逻辑。动态 `skill_bodies` 与 `activate_skill` 需在每轮或绑定 Runnable 时更新 tools 列表（与当前「去枚举防重复 load_skill」行为一致）。
  【Agent 循环】若用 `AgentExecutor`：用 `max_iterations`/`handle_parsing_errors` 对齐 `MAX_TOTAL_ROUNDS`/`MAX_TOOL_ROUNDS`（后者需在 tool 上限后移除 tools 或 middleware 强制文本，与现逻辑一致）。若用 LangGraph：用节点显式复刻「写 DB 用干净 tool 内容、内存 messages 带 TOOL_EMPTY_HINT」的分叉，最易保持与 SQLite 历史一致。
  【历史与 Session】保留 `ChatHistory` 为真源时：在每轮前后做 `dict` ↔ `BaseMessage` 转换，或实现自定义 `BaseChatMessageHistory` 读写 SQLite；评估 `trim_messages` 替代 `_load_truncated_history`，并保留 `_collect_skill_pairs` 防孤立 tool 消息。若改用 LangGraph checkpointer，需迁移或双写 session 模型。
  【用户记忆】`<user_context>` 注入与 `extract_memories` 可保留在链外：pre-hook 拼 system、post-hook 调现有 `extract_memories`；或拆成 LC Runnable 前后步骤。
  【CLI / handlers】`handlers.make_agent`/`run_query` 改为构造 LC Agent/Graph 或 Facade，对外仍暴露 `run(user_input)->str` 与 `session_id`、`activate_skill`、`last_usage`（usage 从 LC 回调或 response_metadata 汇总）。
  【测试】`tests/test_agent.py` 大量 patch `chat`：改为 mock `bind_tools` 的 fake LLM、或 LangGraph 状态机单测；补集成测确认 tool 轮次上限与 DB 写入与现行为一致。
  【风险与顺序】厂商特性（thinking、tool_choice、流式）与 LC 抽象不完全对齐 → 建议 PoC：单 provider + 两工具 + 历史截断 + 一轮迁移测试通过后再扩多厂商与 thinking。

38. 使用 Auto-GPT 实现
-
a. Agent S
  一个 GUI/Computer-Use Agent, an open agentic framework that uses computers like a human
b. Harness Engineering
  prompt/context 的加强版
c. Agentic Search
  让 LLM 本身参与决策——它会思考"我需要搜什么"、执行搜索、判断"够了吗？"、必要时再搜
  ReAct 模式（Reason → Act → Observe → Reason…）就是 Agentic Search 的典型实现方式
d. CoT
  Chain of Thought: 让 LLM 本身一步一步地思考，而不是直接给出答案，如 CoT 的 prompt 示例：
e. Plan and Execute
  先整体规划（Plan），再按步骤执行（Execute）
f. happy horse/ see dance/ nano banana
g. Hermes Agent
-