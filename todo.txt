ToDo List：
1. 对话记忆 => Done
  目标： 将 messages 历史持久化到 SQLite，重启后仍可延续上下文，支持多轮跨会话对话

2. 知识库未命中时自动联网搜索 => Done
  目标： 当 `search_knowledge` 返回空或相似度过低时，通过指令（对话）触发 LLM 自行搜索，
    并提示 LLM 优先在国内网站查找信息，而非直接返回"暂无内容"。

3. 重排序（Reranking）=> Done
  目标： 在向量检索结果上加 Cross-Encoder 二次排序，提升精度
  背景:
    当前 retriever.py 采用两个 Bi-Encoder（SentenceTransformer）+ round-robin 合并：
      · all-MiniLM-L6-v2     → 英文向量化入库 / 召回 kb_en collection
      · BAAI/bge-small-zh    → 中文向量化入库 / 召回 kb_zh collection
    Bi-Encoder 速度快但精度有限——它将 query 和 document 各自单独编码，
    无法捕捉两者之间的细粒度语义交互。

    Reranking 引入第三个模型 Cross-Encoder（ms-marco-MiniLM-L-6-v2）作为二阶段精排：
      · 三个模型职责不同，互不替代，同时工作：
          阶段一（召回）：两个 Bi-Encoder 各自查询 → round-robin 合并取 top_k×3 候选
          阶段二（精排）：Cross-Encoder 对候选逐对打分 → 降序重排 → 截取最终 top_k 条
      · RERANKER_ENABLED=false 时跳过阶段二，两个 Bi-Encoder 行为完全不变（向后兼容）

    三个模型对比：
      模型                       职责          输入          特点
      all-MiniLM-L6-v2          英文召回       单个文本      速度快，支持批量入库
      BAAI/bge-small-zh         中文召回       单个文本      速度快，支持批量入库
      ms-marco-MiniLM-L-6-v2    精排打分       (query, doc)  精度高，不可用于入库

4. 定制 Prompt => Done
  目标：
    - 支持配置文件(./advanced/prompts/prompt_name.prompt.md)， prompt_name.prompt.md 为用户自定义 prompt.
    - 该 prompt 覆盖 Agent 的系统提示(SYSTEM_PROMPT)，适配不同业务场景（5G专家、代码助手等）。
    - 系统启动时检查./advanced/prompts目录，把目录下的prompt 添加为可用命令，只添加一次，不要重复
    - 检查./advanced/prompts目录与可用命令的一致性，删除掉的prompt对应的命令也要删除
    - 用法：用户输入 "/prompt_name [问题（可选）]""
    - 该功能类似 github copilot 的配置 .github/prompts/prompt_name.prompt.md

5. 支持 Skills => Done
  目标： 
    符合 skills 规范， 参考: https://agentskills.io/specification
    如何支持 skills 参考: https://agentskills.io/client-implementation/adding-skills-support
    本项目 skills 目录： advanced/skills/


6. MCP 工具服务器接入
  目标： 接入 Model Context Protocol（MCP）标准，允许 Agent 调用外部 MCP Server 提供的工具，实现与第三方服务的标准化集成


7. 用户自定义 Workflow
  目标： 支持用户通过配置文件或脚本（如 YAML/DSL）自定义工作流，编排多步骤工具调用和 LLM 调用序列，
    实现无需编写代码即可完成复杂自动化任务。
    注：与 no.5「定制 Skills」的边界 —— Skills 管理的是单个工具的描述和参数；Workflow 管理的是
    多个工具/LLM 的调用顺序和条件分支，属于更高层的编排逻辑。前置依赖 no.5 完成后再实现。


8. 优化 ReAct => Done
  目标： 提升 Agent 在多轮对话中的推理能力和工具调用效率，具体包括：
    · 动态工具选择策略：根据问题类型优化 System Prompt，引导 LLM 更准确地决策何时调用哪个工具
    · 工具调用失败重试：工具执行出错时自动重试或降级，而非直接返回失败
    · 多步骤任务规划：支持 LLM 在单次 run() 中拆解复杂任务并按步骤执行
    · 工具结果质量判断：LLM 能识别工具返回内容为空/无关时主动切换策略，而非直接生成回答

9. 多Agent协作 A2A
  目标： 支持多个 Agent 协同工作，共享知识和任务，提高整体效率


10. Web UI
  目标： 接入 Gradio 或 Streamlit，提供图形界面


11. 支持企业级向量数据库
  目标： 支持接入企业级向量数据库，如 Pinecone、Weaviate 等，提高数据存储和检索能力


12. Ollama
  目标： 支持本地 Ollama 模型，完全离线运行


13. URL 批量导入
  目标： 支持通过配置文件批量爬取指定网页并入库


14. 文档自动同步
  目标： 用 `watchdog` 监听 `docs/` 变化，自动增量入库


15. API/SDK接口
  目标： 方便第三方系统集成


16. 安全与权限管理
  目标： 适合企业/团队场景


17. 多语言支持
  目标： 适应国际化需求


18. 本地工具扩展
  目标： 开发更多工具，扩充 Agent 工具集，增强处理复杂任务的能力
  --> 工具扩展后，使用工具的 prompt, skills, reAct 等要对应优化
  候选工具清单：
    a. web_search：集成 Bing Search API 或 DuckDuckGo，直接返回搜索结果摘要+URL列表，
       比 fetch_url 更高效（不需抓取全页，直接得到摘要）。可作为未来自动搜索的工具基础。
       实现方案：
         · 首选：Bing Search API（国内可访，安全密起环境变量 BING_SEARCH_API_KEY）
         · 备选：duckduckgo-search 库（无需 Key，但需代理）
    b. run_python：在沙筱环境中执行 Python 代码片段，返回输出结果
    c. read_file：读取本地文件内容（如日志、csv），支持行范围指定
    d. shell_cmd：执行本地 Shell 命令，返回 stdout/stderr


19. 使用Tab键补全命令 => Done
  CLI 输入命令时支持Tab键补
20. Agent 优化 => Done
  目标：
    提升推理准确性
    提高推理性能，考虑： 减少推理时间，CPU/内存消耗，尽量节省token(保证推理质量，寻找最佳性价比)
    Clean Code
  
21. 优化 session 信息 => Done
  目标：
    使用/session <id> 命令时，切回到原来的 prompt_name， 而不是"你"
    /session 信息中，增加一列显示对应的 prompt_name， 如"5g-expert"
    /session 切换命令中，提示 session id 只需显示前8位，后面加上"首问 "，用户更加容易选择要切换的session

22. tests 优化 => Done
  目标：
    统一 test_xxx.py 命名风格，使命名更common 和 符合命名习惯， 去除“phaseN”。
    精简合并case,只保留必要case,节省测试资源。
    重构case, 使 cases 那模块分组。
23. 统计tokens => Done
  目标： 增加在每次对话结束时，显示当前对话消耗的 token数。
24. 添加 UT 测试脚本 => Done
  脚本类型：shell
  脚本位置和名称：workspace_root/t.sh
  用户可以运行 t [parameter] 来运行对应的UT
  脚本命令 分别对应以下测试case
  t -h : 显示支持的命令
  t -all: .venv\Scripts\python -m pytest
  t -not: .venv\Scripts\python -m pytest -m "not integration" -v
  t -module_name: 分别对应下面的 case
    .venv\Scripts\python -m pytest tests/test_llm.py           # LLM 配置 & Provider
    .venv\Scripts\python -m pytest tests/test_parser.py        # 文档解析（7 种格式）
    .venv\Scripts\python -m pytest tests/test_rag.py           # 分块 & 双语检索 & Reranker
    .venv\Scripts\python -m pytest tests/test_tools.py         # 工具层（search/fetch）
    .venv\Scripts\python -m pytest tests/test_agent.py         # Agent ReAct 循环
    .venv\Scripts\python -m pytest tests/test_memory.py        # 对话记忆持久化
    .venv\Scripts\python -m pytest tests/test_prompt_loader.py # 自定义 Prompt 加载
25. 对话导出 => Done
  实现/save <文件名>命令，把当前 session 完整对话记录存储到 history/文件名.md
26. 支持 Thinking => Done
  目标：
    1. 增加 Extended Thinking 支持，是否开启用户可选
    2. 代码框架可扩展，为兼容各种LLM 抽象 common 接口,如 def call_with_thinking(provider, prompt, budget=8000)
      budget_tokens 推荐： 
      简单推理: 1,024 ~ 3,000, 
      复杂分析: 8,000 ~ 16,000
      高难度Agent 任务: 32,000+
    3. 思考过程流式输出
    注意： thinking 内容不应该被用来做 prompt injection 的载体

27. Agentic Search vs RAG
28. Adaptive Thinking => Done
  目标：
    根据问题复杂度自动估算合适的 thinking budget_tokens，替代固定值：
      · LOW    (1 500)  — 短问、简单事实类（< 25 字符，或含"什么是"等低复杂度词）
      · MEDIUM (8 000)  — 分析、解释、对比（默认档）
      · HIGH   (32 000) — 架构、规划、多步骤深度推理（含"设计/架构/规划"等词,或 > 200 字符）
  实现：
    · estimate_thinking_budget(messages, max_budget) — 在 provider.py 中
    · Agent.__init__ 新增 thinking_adaptive: bool 参数，来自 config.THINKING_ADAPTIVE
    · Agent.run() 当 thinking_adaptive=True 时调用 estimate_thinking_budget，结果传给 call_with_thinking
    · CLI：/thinking adaptive 开启；/thinking 状态显示自适应档位
    · .env.example 新增 THINKING_ADAPTIVE=false
29. Harness Engineering Vs prompt/context
30. agent S: a new agent
-